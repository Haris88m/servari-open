# Decisions (demo)

A neutral demo memory file the Memory surface lists.

## Autonomy defaults
Agents default to L2 (act-then-report); the operator owns the dial.

## Reliability
Every API route degrades gracefully and never crashes the server.

## Data
The shell ships with demo-data/ so it renders on first run.
