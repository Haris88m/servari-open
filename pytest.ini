[pytest]
; SERVARI pytest suite — stdlib + pytest only, no network, no real keys.
; tests/byom_smoke.py is the e2e harness (run directly: python tests/byom_smoke.py)
; and is intentionally outside the test_*.py collection pattern.
testpaths = tests
python_files = test_*.py
